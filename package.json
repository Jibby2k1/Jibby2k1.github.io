{
  "name": "raulv-dev-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/build.mjs && python3 -m http.server 8080",
    "images": "node scripts/optimize-images.mjs",
    "brand": "node scripts/make-brand-assets.mjs",
    "check:budgets": "node scripts/check-budgets.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:smoke": "node scripts/smoke.mjs",
    "check:a11y": "node scripts/check-a11y.mjs",
    "check:freshness": "node scripts/check-freshness.mjs",
    "check": "npm run check:budgets && npm run check:links && npm run check:smoke && npm run check:a11y"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "playwright": "^1.60.0",
    "sharp": "^0.34.5"
  }
}
